pipeline {
    agent {
        label 'electronics'
    }

    stages {

        stage('Tools Configure') {
            steps {
                echo "Tools Installation Pipeline"
            }
        }

        stage('List Update') {
            steps {
                sh '''
                    sudo apt-get update -y
                '''
            }
        }

    }
}